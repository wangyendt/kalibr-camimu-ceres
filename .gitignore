/build/
/out/
/.DS_Store
/.cache/
/external/
/cmake-build-*/
/simulation/generated/
/*.csv
/*.txt
# /*.txt above is meant for stray root-level data dumps, but it also swallows
# the project's root CMakeLists.txt (which left fresh clones unbuildable). Keep
# the build file tracked explicitly.
!/CMakeLists.txt
__pycache__/
/.claude/settings.local.json
